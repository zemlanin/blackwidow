stages:
- dist
- cleanup

before_script:
- vagga _init_storage_dir blackwidow-dist

dist:
  stage: dist
  only: [master]
  script:
  - vagga dist
  - gitlab-publish dist bwd

cleanup:
  stage: cleanup
  script:
  - vagga _clean --unused
